func main() : int {
    int i = 0
    int j = 10

    func inner() : void {
        i = i + 1
        puts i + j
    }

    func jnner(int ct) : void {
        i = i * ct
        puts i
    }

    inner()
    jnner(3)
    jnner(2)
    jnner(2)
    jnner(2)
    inner()

    return 0
}
